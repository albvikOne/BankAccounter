@model IEnumerable<BankAccounter.Models.Bank>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.bankName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.bankLocation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.bankName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.bankLocation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.bankID }) |
            @Html.ActionLink("Details", "Details", new { id=item.bankID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.bankID })
        </td>
    </tr>
}

</table>
